/**
 * Flui.cloud API
 *
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
import { ApplicationScalingDto } from './applicationScalingDto';
import { ApplicationResourcesDto } from './applicationResourcesDto';
import { EnvVarDto } from './envVarDto';


export interface CreateApplicationDto { 
    /**
     * Human-readable name
     */
    name: string;
    /**
     * Optional description
     */
    description?: string;
    category: CreateApplicationDto.CategoryEnum;
    /**
     * Macro-category for top-level menu placement. Defaults to APPLICATION when omitted.
     */
    kind?: CreateApplicationDto.KindEnum;
    sourceType: CreateApplicationDto.SourceTypeEnum;
    /**
     * Kubernetes namespace
     */
    k8sNamespace?: string;
    /**
     * Polymorphic source configuration based on sourceType
     */
    sourceConfig: object;
    /**
     * Environment variables
     */
    env?: Array<EnvVarDto>;
    /**
     * CPU and memory limits
     */
    resources?: ApplicationResourcesDto;
    /**
     * Autoscaling configuration
     */
    scaling?: ApplicationScalingDto;
    /**
     * Number of replicas
     */
    replicas?: number;
    /**
     * Main container port
     */
    port?: number;
    /**
     * K8s-style labels
     */
    labels?: object;
    /**
     * Flexible metadata
     */
    metadata?: object;
    /**
     * Named resource profile for CPU/RAM allocation. Ignored if \"resources\" is also specified (raw values take priority). If neither is provided, defaults to \"small\".
     */
    resourceProfile?: CreateApplicationDto.ResourceProfileEnum;
    /**
     * Readiness probe configuration. If not provided, no probe is injected into the Deployment.
     */
    healthProbe?: object;
    /**
     * Optional persistent volumes. Each entry produces a PVC and is mounted at mountPath inside the container.
     */
    volumes?: Array<Array<string>>;
    /**
     * Kubernetes workload kind. Defaults to Deployment.
     */
    workloadKind?: CreateApplicationDto.WorkloadKindEnum;
    /**
     * Storage placement: `shared` (default) for stateless apps using NFS layer; `dedicated` pins the pod to the master node so writes hit the backing Volume directly (required by databases where NFS breaks fsync/locking).
     */
    persistenceScope?: CreateApplicationDto.PersistenceScopeEnum;
    /**
     * Target k8s node name when `persistenceScope=dedicated`. If omitted, the app is pinned to the master (control-plane) node. When set, the named worker becomes \"locked\" (no drain, no scale-down) for as long as it hosts the app.
     */
    dedicatedNodeName?: string;
    /**
     * Override the container start command. Used to wrap/compose env vars before exec (e.g. catalog clients that need DATABASE_URL composed from PGHOST/PGUSER/PGPASSWORD at runtime).
     */
    startCommand?: string;
    /**
     * If true, automatically trigger a deploy immediately after creation. The response will include an operation object for tracking progress.
     */
    autoDeploy?: boolean;
    /**
     * How the app is reached. \"public\" (default) creates Ingress + Certificate + DNS on a public hostname. \"internal\" skips all public exposure: only Deployment + Service ClusterIP are created, and the app is reachable only from the Flui dashboard through the ForwardAuth proxy.
     */
    exposure?: CreateApplicationDto.ExposureEnum;
    /**
     * ID of a completed standalone build to create the application from. When provided, the app is created atomically and a deploy is triggered immediately. The build must be COMPLETED and not yet linked to any application.
     */
    buildId?: string;
}
export namespace CreateApplicationDto {
    export const CategoryEnum = {
        System: 'system',
        User: 'user'
    } as const;
    export type CategoryEnum = typeof CategoryEnum[keyof typeof CategoryEnum];
    export const KindEnum = {
        Database: 'DATABASE',
        Application: 'APPLICATION',
        Tool: 'TOOL',
        System: 'SYSTEM'
    } as const;
    export type KindEnum = typeof KindEnum[keyof typeof KindEnum];
    export const SourceTypeEnum = {
        DockerImage: 'docker_image',
        GitBuild: 'git_build',
        HelmChart: 'helm_chart',
        RawManifest: 'raw_manifest'
    } as const;
    export type SourceTypeEnum = typeof SourceTypeEnum[keyof typeof SourceTypeEnum];
    export const ResourceProfileEnum = {
        Nano: 'nano',
        Small: 'small',
        Medium: 'medium',
        Large: 'large',
        Xlarge: 'xlarge'
    } as const;
    export type ResourceProfileEnum = typeof ResourceProfileEnum[keyof typeof ResourceProfileEnum];
    export const WorkloadKindEnum = {
        Deployment: 'Deployment',
        StatefulSet: 'StatefulSet',
        DaemonSet: 'DaemonSet'
    } as const;
    export type WorkloadKindEnum = typeof WorkloadKindEnum[keyof typeof WorkloadKindEnum];
    export const PersistenceScopeEnum = {
        Shared: 'shared',
        Dedicated: 'dedicated'
    } as const;
    export type PersistenceScopeEnum = typeof PersistenceScopeEnum[keyof typeof PersistenceScopeEnum];
    export const ExposureEnum = {
        Public: 'public',
        Internal: 'internal'
    } as const;
    export type ExposureEnum = typeof ExposureEnum[keyof typeof ExposureEnum];
}


