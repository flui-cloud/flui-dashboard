/**
 * Flui.cloud API
 *
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


export interface DeployFromYamlDto { 
    /**
     * Raw flui.yaml content (kind: Application)
     */
    yaml: string;
    /**
     * Target cluster UUID
     */
    clusterId: string;
    /**
     * GitHub repository full name (owner/repo)
     */
    repoFullName: string;
    /**
     * Git branch to deploy from
     */
    branch?: string;
    /**
     * Environment variable overrides (KEY=value map)
     */
    envOverrides?: object;
    /**
     * Validate manifest without triggering a deploy
     */
    validateOnly?: boolean;
    /**
     * Skip the GitHub Actions build step and re-deploy the current imageRef. Useful for fast iterations on the manifest config (env, ports, healthcheck, endpoint) without rebuilding the image. If the app was deleted, falls back to GHCR latest-tag lookup for {owner}/{repoName}.
     */
    skipBuild?: boolean;
    /**
     * Explicit image reference to deploy (e.g. ghcr.io/owner/repo:sha). Skips the build pipeline. Takes precedence over skipBuild auto-discovery. Use for rollback to a specific tag, or to deploy a known image when the app was deleted from Flui.
     */
    imageRef?: string;
}

