/**
 * Flui.cloud API
 *
 * NOTE: Mirrors the backend ProviderFirewallDto. Normally generated by OpenAPI
 * Generator, but hand-added forward-compatibly while the swagger plugin emits an
 * empty schema for this class — a future `api:generate` produces the same shape.
 */


export interface ProviderFirewallDto {
    backend: ProviderFirewallDto.BackendEnum;
    managedEdge: boolean;
    supportsSshAllowlist: boolean;
}
export namespace ProviderFirewallDto {
    export const BackendEnum = {
        ManagedApi: 'managed-api',
        HostNftables: 'host-nftables',
        None: 'none'
    } as const;
    export type BackendEnum = typeof BackendEnum[keyof typeof BackendEnum];
}
