/**
 * Flui.cloud API
 *
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
import { VNetConfigDto } from './vNetConfigDto';


export interface CreateClusterDto { 
    /**
     * Cluster name (must be unique)
     */
    name: string;
    /**
     * Cloud provider
     */
    provider: CreateClusterDto.ProviderEnum;
    /**
     * Region/location code
     */
    region: string;
    /**
     * Node size/type (applies to all nodes)
     */
    nodeSize: string;
    /**
     * Number of worker nodes (0 = master-only, max 19 workers)
     */
    workerCount: number;
    /**
     * Enable autoscaling
     */
    autoscalingEnabled?: boolean;
    /**
     * Minimum number of nodes (for autoscaling)
     */
    minNodes?: number;
    /**
     * Maximum number of nodes (for autoscaling)
     */
    maxNodes?: number;
    /**
     * Memory utilization percentage that triggers a scale-up. Overrides global default.
     */
    scaleUpMemoryPct?: number;
    /**
     * CPU utilization percentage that triggers a scale-up. Overrides global default.
     */
    scaleUpCpuPct?: number;
    /**
     * Cooldown period (seconds) between consecutive scale events. Overrides global default.
     */
    cooldownSeconds?: number;
    /**
     * K3s version (default: v1.35.4+k3s1)
     */
    k3sVersion?: string;
    /**
     * SSH key IDs for node access
     */
    sshKeys?: Array<string>;
    /**
     * OS image (default: ubuntu-24.04)
     */
    image?: string;
    /**
     * Additional metadata
     */
    metadata?: object;
    /**
     * Firewall rules for the cluster (desired state). If not provided, cluster will be created with empty firewall (deny-all). Provide an array of firewall rules to define the cluster firewall protection.
     */
    firewallRules?: Array<object>;
    /**
     * Disk size in GB for each node. Required for node types with network storage (storageType=network, e.g. Scaleway PRO2, ENT1). For local SSD types the value is ignored. Minimum recommended: 20 GB.
     */
    diskSizeGb?: number;
    /**
     * VNet configuration for cluster nodes. If provided, all cluster nodes (master and workers) will be attached to the specified VNet. The VNet must exist in the same provider and region as the cluster.
     */
    vnetConfig?: VNetConfigDto;
    /**
     * Default hostname source for endpoints (system services and user apps). IP uses nip.io against the master public IP — zero external DNS required, ideal for testing. DOMAIN requires a DNS zone to be assigned to the cluster afterwards. Defaults to IP.
     */
    endpointHostnameMode?: CreateClusterDto.EndpointHostnameModeEnum;
    /**
     * Optional token injected into nip.io system hostnames (auth/app/api). When omitted, a random token is generated server-side. Ensures each cluster has a unique LE domain set, avoiding rate limits during repeated test cycles.
     */
    nipHostnameToken?: string;
    /**
     * Enable Flui shared storage (NFS+fscache, see scaling doc §14). When true (default), the master gets an attached Volume that hosts the NFS export, and workers mount it via NFSv4 with cachefilesd. Disable to fall back to local-path on each node bundled disk.
     */
    sharedStorageEnabled?: boolean;
    /**
     * Size in GB of the master backing Volume hosting the NFS export. Hetzner minimum is 10 GB.
     */
    sharedStorageVolumeSizeGb?: number;
}
export namespace CreateClusterDto {
    export const ProviderEnum = {
        Contabo: 'contabo',
        Hetzner: 'hetzner',
        Scaleway: 'scaleway'
    } as const;
    export type ProviderEnum = typeof ProviderEnum[keyof typeof ProviderEnum];
    export const EndpointHostnameModeEnum = {
        Ip: 'ip',
        Domain: 'domain'
    } as const;
    export type EndpointHostnameModeEnum = typeof EndpointHostnameModeEnum[keyof typeof EndpointHostnameModeEnum];
}


