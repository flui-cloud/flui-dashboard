/**
 * Flui.cloud API
 *
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


export interface MailDomainProofsDto { 
    domain: string;
    /**
     * Absent where this provider asks for no such record — Brevo authenticates on DKIM and publishes no SPF requirement at all. Distinct from `missing`, which means it asked and the record is not there. `pending` means the provider has not accepted the record yet, which is not the same as the record being absent from DNS.
     */
    spf?: MailDomainProofsDto.SpfEnum;
    dkim?: MailDomainProofsDto.DkimEnum;
    dmarc?: MailDomainProofsDto.DmarcEnum;
    verified: boolean;
    /**
     * Which connection holds this domain. Without it a list covering two providers cannot say whose account a domain lives in — and a domain only exists as one account’s registration, so it is not an attribute of the platform but of the connection.
     */
    provider: string;
    scope: MailDomainProofsDto.ScopeEnum;
    /**
     * Whether that connection is the one currently carrying its scope.
     */
    active: boolean;
    connectionId: string;
}
export namespace MailDomainProofsDto {
    export const SpfEnum = {
        Ok: 'ok',
        Missing: 'missing',
        Mismatch: 'mismatch',
        Pending: 'pending'
    } as const;
    export type SpfEnum = typeof SpfEnum[keyof typeof SpfEnum];
    export const DkimEnum = {
        Ok: 'ok',
        Missing: 'missing',
        Mismatch: 'mismatch',
        Pending: 'pending'
    } as const;
    export type DkimEnum = typeof DkimEnum[keyof typeof DkimEnum];
    export const DmarcEnum = {
        Ok: 'ok',
        Missing: 'missing',
        Mismatch: 'mismatch',
        Pending: 'pending'
    } as const;
    export type DmarcEnum = typeof DmarcEnum[keyof typeof DmarcEnum];
    export const ScopeEnum = {
        Transactional: 'transactional',
        Bulk: 'bulk'
    } as const;
    export type ScopeEnum = typeof ScopeEnum[keyof typeof ScopeEnum];
}


