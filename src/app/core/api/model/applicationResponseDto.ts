/**
 * Flui.cloud API
 *
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
import { AppOperationResponseDto } from './appOperationResponseDto';


export interface ApplicationResponseDto { 
    id: string;
    name: string;
    slug: string;
    description?: string;
    category: ApplicationResponseDto.CategoryEnum;
    /**
     * Macro-category that drives top-level menu placement.
     */
    kind: ApplicationResponseDto.KindEnum;
    sourceType: ApplicationResponseDto.SourceTypeEnum;
    clusterId: string;
    /**
     * Owning project id (null if unassigned)
     */
    projectId?: string | null;
    k8sNamespace: string;
    status: ApplicationResponseDto.StatusEnum;
    reconciliationStatus: ApplicationResponseDto.ReconciliationStatusEnum;
    lastReconciliationAt?: string;
    reconciliationError?: string;
    sourceConfig: object;
    env: Array<string>;
    resources: object;
    scaling: object;
    replicas: number;
    port?: number;
    currentRevisionId?: string;
    imageRef?: string;
    /**
     * Effective container start command. Null = use image CMD. User-overridable.
     */
    startCommand?: string;
    userId?: string;
    systemProtected: boolean;
    /**
     * When true, new builds automatically trigger a deploy
     */
    autoDeploy: boolean;
    /**
     * How the app is reached. \"public\" exposes the app via Ingress + Certificate + DNS on a public hostname (external endpoint with its own domain). \"internal\" means no public exposure: only Deployment + Service ClusterIP exist, and the app is reachable only from the Flui dashboard through the ForwardAuth proxy. Frontend should hide the DNS / domain / certificate tabs when this is \"internal\".
     */
    exposure: ApplicationResponseDto.ExposureEnum;
    /**
     * Fully-qualified public access URL for a `public` app, composed from the app endpoint hostname as `https://<fqdn><entrypointPath>`. This is the real, authoritative link — consumers (dashboard \"Open\" button, the assistant) MUST use it verbatim and never reconstruct it from the slug. Undefined for internal apps (use `internalUrl`) and for public apps whose endpoint is not provisioned yet.
     */
    url?: string;
    /**
     * Fully-qualified URL the dashboard should use for the \"Open\" button when this is an internal app. Composed as `https://<slug>.internal.<clusterZone><entrypointPath>`. Populated only on detail responses (GET /applications/:id and after-create/after-update flows) and only when the cluster currently supports internal hosting (capabilities.hasInternalHosting === true). Undefined for public apps and for internal apps on clusters that do not yet have internal hosting configured — in the latter case the FE must keep the button disabled.
     */
    internalUrl?: string;
    /**
     * ID of the currently active revision (alias of currentRevisionId)
     */
    activeRevisionId?: string | null;
    /**
     * Kubernetes workload kind generated by Flui. Most apps are Deployments; building-block apps with volumes use StatefulSet so the PVC follows the pod identity.
     */
    workloadKind: ApplicationResponseDto.WorkloadKindEnum;
    /**
     * Where the app\'s storage lives. \"shared\" lets the PVC ride on the cluster-wide flui-shared volume (works on any node). \"dedicated\" pins the pod to one node so the PVC uses local disk on that node — required by databases that need real fsync/locking guarantees.
     */
    persistenceScope: ApplicationResponseDto.PersistenceScopeEnum;
    /**
     * When persistenceScope=dedicated, the Kubernetes node name the pod is pinned to. Null until the deploy auto-assigns the worker with the most free capacity.
     */
    dedicatedNodeName?: string | null;
    /**
     * When persistenceScope=dedicated, whether the app may schedule on the master (control-plane) node instead of a worker. Defaults to false.
     */
    allowMasterPlacement: boolean;
    labels: object;
    metadata: object;
    lastDeployedAt?: string;
    buildPath?: string;
    frameworkConfirmed?: string;
    workflowRunId?: string;
    /**
     * Fully-qualified HTML URL of the workflow run on GitHub, cached when the run is first observed. Frontend can link to this directly without resolving owner/repo itself.
     */
    workflowRunUrl?: string;
    /**
     * Timestamp at which the application entered AWAITING_BUILD. Useful to render \"building for Xm\" and to warn the user when approaching the 30-minute build timeout.
     */
    buildStartedAt?: string;
    /**
     * Snapshot of the GitHub Actions workflow status from the last watcher tick. Frontend should render build-phase UI from this, not from GET /workflow-status (which is a non-canonical pass-through).
     */
    lastBuildStatus?: ApplicationResponseDto.LastBuildStatusEnum;
    /**
     * Cached conclusion of the last completed workflow run. Null while the build is still queued or in progress.
     */
    lastBuildConclusion?: ApplicationResponseDto.LastBuildConclusionEnum;
    /**
     * Most recent deploy/build operation for this application
     */
    lastOperation?: AppOperationResponseDto;
    /**
     * Catalog slug when this app was installed via the catalog (e.g. \"postgresql\", \"vaultwarden\"). Undefined for apps created outside the catalog. Drives the \"Installed · N\" badge and \"Your instances\" cross-reference on the catalog pages.
     */
    catalogSlug?: string;
    /**
     * Id of the CatalogInstallEntity that created this app. Use it to deep-link from the application detail page back to its catalog install (progress, linked clients, uninstall flow). Undefined for non-catalog apps.
     */
    catalogInstallId?: string;
    /**
     * Catalog version pinned at install time (mirrors metadata.version of the manifest). Compare against the current catalog definition version to flag \"update available\". Undefined for non-catalog apps.
     */
    catalogVersion?: string;
    /**
     * Set only within a composed-app group response: true for the component that is the face of the bundle (owns the public endpoint). Undefined in flat listings.
     */
    isPrimary?: boolean;
    /**
     * Set only within a composed-app group response: the bundle display name (e.g. \"Nextcloud\") this component belongs to. Undefined in flat listings.
     */
    composedAppName?: string;
    createdAt: string;
    updatedAt: string;
}
export namespace ApplicationResponseDto {
    export const CategoryEnum = {
        System: 'system',
        User: 'user'
    } as const;
    export type CategoryEnum = typeof CategoryEnum[keyof typeof CategoryEnum];
    export const KindEnum = {
        Database: 'DATABASE',
        Application: 'APPLICATION',
        Tool: 'TOOL',
        System: 'SYSTEM'
    } as const;
    export type KindEnum = typeof KindEnum[keyof typeof KindEnum];
    export const SourceTypeEnum = {
        DockerImage: 'docker_image',
        GitBuild: 'git_build',
        HelmChart: 'helm_chart',
        RawManifest: 'raw_manifest'
    } as const;
    export type SourceTypeEnum = typeof SourceTypeEnum[keyof typeof SourceTypeEnum];
    export const StatusEnum = {
        Pending: 'pending',
        AwaitingBuild: 'awaiting_build',
        Provisioning: 'provisioning',
        Running: 'running',
        Degraded: 'degraded',
        Stopped: 'stopped',
        Updating: 'updating',
        RollingBack: 'rolling_back',
        Failed: 'failed',
        Deleting: 'deleting',
        Deleted: 'deleted'
    } as const;
    export type StatusEnum = typeof StatusEnum[keyof typeof StatusEnum];
    export const ReconciliationStatusEnum = {
        Pending: 'PENDING',
        InSync: 'IN_SYNC',
        Drift: 'DRIFT',
        Reconciling: 'RECONCILING',
        Error: 'ERROR'
    } as const;
    export type ReconciliationStatusEnum = typeof ReconciliationStatusEnum[keyof typeof ReconciliationStatusEnum];
    export const ExposureEnum = {
        Public: 'public',
        Internal: 'internal',
        Cluster: 'cluster'
    } as const;
    export type ExposureEnum = typeof ExposureEnum[keyof typeof ExposureEnum];
    export const WorkloadKindEnum = {
        Deployment: 'Deployment',
        StatefulSet: 'StatefulSet',
        DaemonSet: 'DaemonSet'
    } as const;
    export type WorkloadKindEnum = typeof WorkloadKindEnum[keyof typeof WorkloadKindEnum];
    export const PersistenceScopeEnum = {
        Shared: 'shared',
        Dedicated: 'dedicated'
    } as const;
    export type PersistenceScopeEnum = typeof PersistenceScopeEnum[keyof typeof PersistenceScopeEnum];
    export const LastBuildStatusEnum = {
        Queued: 'queued',
        InProgress: 'in_progress',
        Completed: 'completed'
    } as const;
    export type LastBuildStatusEnum = typeof LastBuildStatusEnum[keyof typeof LastBuildStatusEnum];
    export const LastBuildConclusionEnum = {
        Success: 'success',
        Failure: 'failure',
        Cancelled: 'cancelled'
    } as const;
    export type LastBuildConclusionEnum = typeof LastBuildConclusionEnum[keyof typeof LastBuildConclusionEnum];
}


