/**
 * Flui.cloud API
 *
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


export interface AppliedUnitDto { 
    /**
     * The unit directory, `.` for the repo root
     */
    unitId: string;
    /**
     * metadata.name of the rendered manifest
     */
    name: string;
    applicationId: string;
    slug: string;
    /**
     * Where the rendered manifest was committed
     */
    manifestPath: string;
    /**
     * Where this unit’s build workflow was committed
     */
    workflowPath: string;
    /**
     * `AWAITING_BUILD` when the unit was armed, `PENDING` when it was not.
     */
    status: string;
    /**
     * Whether this unit was armed after the commit: the webhook credential stored, the build row opened, the status moved to AWAITING_BUILD. Its build is running either way — the commit that starts every build has already landed — but a unit that is not armed answers its own webhook with a 401 and will never deploy on its own.
     */
    armed: boolean;
    /**
     * Why the unit was not armed. Absent when it was.
     */
    reason?: string;
    /**
     * For a unit that was not armed: whether Flui could mark it so the next apply of this repository reuses it. False means the next apply will not recognise it and will create a second application — with a second database if this one attached any — beside it.
     */
    markedForReuse?: boolean;
    /**
     * The GitHub Actions run this unit’s build started, when it could be resolved in the seconds after the commit. Absent is not a failure: the build watcher finds the run either way.
     */
    workflowRunUrl?: string;
    /**
     * Variables this unit declared as secret and has no value for yet. The names only — a value is delivered by a person, never through this API. Empty when the unit needs nothing.
     */
    pendingInputs: Array<string>;
}

